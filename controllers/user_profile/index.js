const { user_dashboard } = require("./dashboard");
const { user_blog } = require("./user_blogs");
const { profile } = require("./profile");

module.exports = {
  user_dashboard,
  user_blog,
  profile,
};
